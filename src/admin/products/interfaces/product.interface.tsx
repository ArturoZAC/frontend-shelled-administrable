export interface Product {
  id: string;
  title: string;
  description: string;
  image: string;
}

export interface ProductResponse {
  id: string;
  title: string;
  description: string;
  image: string;
}
